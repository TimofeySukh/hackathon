# Production build env (committed on purpose). These are PUBLIC client values —
# the publishable key is the browser-facing anon key, protected by Postgres RLS,
# and is already shipped in the JS bundle. No secrets here.
# Vite auto-loads this file during `vite build`, so the server build works even
# if its deploy.env is missing the vars.
VITE_SUPABASE_URL=https://lycfoukfoesobeuumuad.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_l_x_y5rxdhL8Sd1ZE3QXag_lOCtr_M9
VITE_GOOGLE_CLIENT_ID=614440848285-8er6rqi1ritmmie9sifbb44ce1l5clvl.apps.googleusercontent.com
