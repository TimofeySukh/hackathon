# <Feature name>

> Copy this file to `features/<feature-name>.md`, fill every section, and add it to the
> index in [`README.md`](README.md). Delete this quote block when done.

## Purpose

What this feature is and the user problem it solves. One short paragraph.

## Behavior

How it works from the user's point of view: entry points, interactions, gestures,
states, edge cases. Bullet points are fine.

## Design

How it should look. Reference the global language in
[`../DESIGN_SYSTEM.md`](../DESIGN_SYSTEM.md) instead of restating tokens.

- Surfaces / elevation used:
- Components used (button / switch / menu / card / text field / etc.):
- Color roles used:
- Anything feature-specific (custom shapes, layout rules, motion):
- Known gaps vs. the Material 3 target:

## Code

Where it lives.

- Main file(s):
- Key functions / components:
- Related state / hooks:

## Open questions / TODO

Anything unresolved a future contributor should know.
